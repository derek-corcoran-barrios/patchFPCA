% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-report.R
\name{render_spatial_report}
\alias{render_spatial_report}
\title{Render the spatial-autocorrelation robustness report}
\usage{
render_spatial_report(
  project_root = ".",
  fpca_results_dir = "Results",
  patch_dir = "Species_PatchDistances",
  spatial_output_dir = "Results/spatial_autocorrelation",
  merge_variant = c("20m", "10m", "zero"),
  report_dir = "spatial-report",
  output_file = "patchFPCA_spatial_autocorrelation.pdf",
  quiet = FALSE
)
}
\arguments{
\item{project_root}{Existing project directory containing the analysis
inputs, or to which relative input paths refer.}

\item{fpca_results_dir}{Directory containing \code{patch_fpca_results.rds},
relative to \code{project_root} unless absolute.}

\item{patch_dir}{Directory containing the patch shapefiles and matching
lookup/area tables, relative to \code{project_root} unless absolute.}

\item{spatial_output_dir}{Directory for fitted models and result tables,
relative to \code{project_root} unless absolute.}

\item{merge_variant}{Patch correction used to generate the FPCA inputs.}

\item{report_dir}{Writable directory into which the report sources, cache,
and PDF are copied or generated, relative to \code{project_root} unless
absolute.}

\item{output_file}{PDF filename. Supply a filename, not a directory path.}

\item{quiet}{Passed to \code{rmarkdown::render()}.}
}
\value{
Invisibly, the normalized path to the rendered PDF.
}
\description{
Locates the report bundled with an installed \code{patchFPCA} package, copies the
R Markdown source, helper functions, and bibliography to a writable project
directory, and renders the bookdown PDF there. This avoids relying on a
source-repository path such as \code{inst/analysis/...}.
}
